def cal(word):
    character = (len(word))
    return character

def main():
    while True :
        word = input("Please enter your word")
        print ("The number of character in your word is:" + " " + str(cal(word)))

print(main)